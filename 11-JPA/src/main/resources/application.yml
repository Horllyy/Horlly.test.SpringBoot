spring:
  datasource:
    url: jdbc:mysql://localhost/jpa
    username: root
    password: 123456
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      #update：更新或者创建数据表
      ddl-auto: update
      #控制台显示sql
    show-sql: true